# 阶段总结

本文档用于给后续开发者快速了解项目各阶段的目标、完成内容、复现方式、踩坑经验和继续开发注意事项。后续新增功能时，应优先按“阶段 N”的方式追加记录，而不是只在末尾写零散补充。

## 阶段索引

- 阶段 1：浏览器同源验证版。完成纯静态 WebLLM server/client 验证，保留 BroadcastChannel 方案。
- 阶段 2：Go gateway 主线。实现本地 OpenAI 风格 HTTP API、WebSocket bridge、curl/SDK/HTTP client 调用路径。
- 阶段 3：模型加载兼容性与端口优化。处理 sft、Qwen3.5、Gemma3 等模型差异，加入 21434 起自动寻找可用端口。
- 阶段 4：release 打包与页面双语化。生成可直接运行的发布包，并为主线页面加入中英文切换机制。

## 阶段 1-3：同源验证、Go gateway 主线与模型兼容性

本阶段已经从“纯浏览器同源验证版”推进到“Go gateway + 浏览器 WebLLM 服务端”的本地 OpenAI API 形态。

当前主线能力：

- 浏览器页面 `server.html` 负责加载和运行 WebLLM 模型。
- Go gateway 负责监听本地 HTTP 端口。
- 外部客户端可以通过 OpenAI 风格接口访问本地模型。
- `client.html` 已改为通过 HTTP 调用 gateway，不再要求与 `server.html` 同源。
- 原先的同源 BroadcastChannel 验证版已经保留为独立文件。

## 主线架构

主线通信链路：

```text
外部客户端 / curl / OpenAI SDK / client.html
        ↓ HTTP / SSE
Go gateway: http://127.0.0.1:21434/v1
        ↓ WebSocket
server.html
        ↓ WebLLM / WebGPU
浏览器本地模型推理
```

Go gateway 提供：

- `GET /health`
- `GET /v1/models`
- `POST /v1/chat/completions`
- `GET /bridge`，供 `server.html` 建立 WebSocket 桥接
- 静态文件托管，可直接访问 `server.html` 和 `client.html`

默认地址：

```text
http://127.0.0.1:21434
```

如果默认端口被占用，gateway 会从 `21434` 开始向上寻找可用端口，最多查到 `21534`。实际选中的地址会写入：

```text
webllm-gateway-config.json
```

主线 `server.html` 和 `client.html` 启动时会读取该文件，从而自动使用实际端口。

OpenAI API base URL：

```text
http://127.0.0.1:21434/v1
```

## 运行方式

从项目根目录运行：

```bash
go run .
```

然后打开：

```text
http://127.0.0.1:21434/server.html
http://127.0.0.1:21434/client.html
```

基本使用流程：

1. 启动 Go gateway。
2. 打开 `server.html`。
3. 确认或点击连接 Gateway。
4. 在 `server.html` 中选择并加载模型。
5. 打开 `client.html`，或使用 curl / OpenAI SDK 请求本地 API。

## 保留的同源验证版

为保留纯浏览器验证能力，原来的同源 BroadcastChannel 方案已经独立保存：

- `server-same-origin.html`
- `client-same-origin.html`
- `src/server-same-origin.js`
- `src/client-same-origin.js`

同源版适合：

- 不启动 Go gateway 的浏览器内验证。
- GitHub Pages 等静态托管演示。
- 快速排查 WebLLM 模型加载和基本对话。

同源版限制：

- 不能监听本地端口。
- 不能被 curl、CLI、OpenAI SDK 等外部程序直接调用。
- 只能在同一 origin 的页面之间通信。

## Go Gateway 实现说明

Go gateway 文件：

```text
gateway/main.go
```

项目 Go 模块：

```text
go.mod
```

当前 gateway 使用 Go 标准库实现，没有引入第三方依赖。

实现内容包括：

- HTTP server
- CORS
- JSON 请求和响应
- SSE streaming
- 静态文件服务
- 一个最小 WebSocket bridge

当前已验证：

```bash
go test ./gateway
```

能够通过编译检查。

## Client 改动

主线 `client.html` / `src/client.js` 已经从同源 BroadcastChannel 客户端改为 HTTP OpenAI API 客户端。

当前特点：

- 默认 Base URL 为 `http://127.0.0.1:21434/v1`。
- 不再要求与 `server.html` 同源。
- 支持普通 JSON 响应。
- 支持 streaming SSE 响应。
- 客户端不选择模型，只使用 `server.html` 当前已加载的模型。
- 请求体默认不带 `model` 字段，由服务端当前加载模型决定。
- API Key 在 HTTP 客户端中以 `Authorization: Bearer ...` 形式发送。
- 当前 gateway 接收该 header 以兼容 OpenAI 客户端，但暂不做鉴权。

同源验证版没有真实 HTTP header，`client-same-origin.html` 会把 API Key 放入请求的 `metadata.api_key`，主要用于日志观察和保持请求形状。

## Server 页面改动

主线 `server.html` / `src/server.js` 继续负责 WebLLM 推理，并新增 Gateway 连接能力。

当前能力：

- 加载 WebLLM 模型。
- 显示模型加载进度。
- 显示请求和响应日志。
- 自动或手动连接 `ws://127.0.0.1:21434/bridge`。
- 接收 gateway 转发的 OpenAI 风格请求。
- 返回普通响应或流式 chunk。

## 模型兼容处理

当前模型配置在：

```text
src/models.js
```

### 缓存后端

缓存后端当前统一使用 WebLLM 的 `indexeddb` cache backend。

原因：

- 之前同源验证版本使用 IndexedDB 时模型加载正常。
- 切换到浏览器默认 Cache API 后，当前环境中会遇到：

```text
Failed to execute 'add' on 'Cache': Cache.add() encountered a network error
```

- Qwen3.5 在加载中途刷新页面后，IndexedDB 可能留下损坏的大对象，之后可能出现：

```text
Failed to read large IndexedDB value
```

因此当前选择是：优先保证模型能正常下载，统一使用 IndexedDB；如果中断刷新造成 IndexedDB 损坏，需要使用 `server.html` 中的 `清理模型缓存` 按钮，或手动清理当前站点浏览器存储后重新下载。

已处理的重要模型兼容问题：

### sft_model_1.5B

模型：

```text
sft_model_1.5B-q4f16_1-MLC (Hugging Face)
```

注意事项：

- 使用自定义 `ModelRecord`。
- 权重来自 Hugging Face：

```text
https://huggingface.co/yoaocopy/sft_model_1.5B-q4f16_1-MLC
```

- `model_lib` 使用 `v0_2_48` 的 Qwen2 1.5B wasm。
- 该模型使用 `@mlc-ai/web-llm@0.2.79` 运行时。
- 如果用较新的 WebLLM 运行时，可能出现 wasm ABI 不兼容错误。

### Qwen3.5

模型：

```text
Qwen3.5-0.8B-q4f16_1-MLC
Qwen3.5-2B-q4f16_1-MLC
```

注意事项：

- 需要较新的 WebLLM runtime。
- 当前主线中，非 sft 模型默认使用 `@mlc-ai/web-llm@0.2.83`。
- 如果用旧 runtime，可能出现：

```text
Cannot find model record in appConfig
```

### Gemma3

模型：

```text
gemma3-1b-it-q4f16_1-MLC
```

曾遇到的问题：

```text
Only one of context_window_size and sliding_window_size can be positive.
```

处理方式：

- 不手写或猜测 `model_lib`。
- 继承 WebLLM 内置记录的 `model` 和 `model_lib`。
- 只通过 `overrides` 修正冲突字段：

```js
overrides: {
  context_window_size: 4096,
  sliding_window_size: -1,
}
```

## ModelRecord 合并规则

当前 `src/server.js` 中的 appConfig 合并逻辑遵循：

- 如果自定义 `model_id` 不存在于 WebLLM 内置列表，则追加。
- 如果自定义 `model_id` 已存在，则合并到内置记录。
- 默认保留内置记录的 `model` 和 `model_lib`。
- 只在自定义记录显式提供字段时覆盖。
- `overrides` 会浅合并。

这个规则对 Gemma3 很重要，因为 Gemma3 只需要覆盖 `overrides`，不应该替换内置 `model_lib`。

## 采样参数原则

当前服务端不主动覆盖采样参数。

也就是说：

- `temperature`
- `top_p`
- `max_tokens`
- `stop`
- 其他生成参数

都应该来自 `client.html` 或外部 OpenAI 请求。

如果某些模型需要特殊 stop token 或重复惩罚，优先通过客户端页面或请求参数显式传入，而不是在服务端偷偷按模型族改写。

## 已验证内容

本阶段完成后已做的检查：

```bash
go test ./gateway
node --check src/server.js
node --check src/client.js
node --check src/server-same-origin.js
node --check src/client-same-origin.js
```

以上检查均通过。

尚未完成的验证：

- 未做完整浏览器端到端测试。
- 未在真实 WebGPU 环境中通过 gateway 完整跑一次模型加载和聊天。
- 未测试外部 OpenAI SDK 调用。

## 后续建议

下一步建议：

1. 在浏览器中完整测试 Go gateway 模式：
   - 启动 gateway
   - 打开 `server.html`
   - 加载模型
   - 使用 `client.html` 发送请求
   - 测试 stream 和 non-stream
2. 使用 curl 验证 `/v1/chat/completions`。
3. 使用 Python 或 Node 的 OpenAI SDK 验证兼容性。
4. 根据测试结果补齐 OpenAI 返回格式细节。
5. 增加 gateway 的命令行参数说明和跨平台构建脚本。
6. 后续考虑发布单文件可执行程序。
## 阶段 4：release 打包与页面双语化

本阶段主要完成两类开发体验优化：一是让 gateway 可以生成“下载后直接运行”的发布包，二是给主线页面加入轻量中英文切换。

### 单文件 gateway 内嵌静态资源

后续又将 gateway 调整为默认内嵌静态页面资源的单文件可执行程序：

- 仓库根目录新增 `main.go`，使用 Go `embed` 打包：
  - `index.html`
  - `server.html`
  - `client.html`
  - `server-same-origin.html`
  - `client-same-origin.html`
  - `src/*`
- `gateway/main.go` 从 `package main` 改为 `package gateway`，暴露 `Run(staticFiles http.FileSystem)`。
- 构建命令从 `go build ./gateway` 改为 `go build .`。
- 源码运行命令从 `go run ./gateway` 改为 `go run .`。
- 默认运行时使用内嵌静态文件，因此最终二进制可以单独复制到其他目录运行。
- 开发前端页面时，`go run .` 默认从当前工作目录读取页面文件，适合即时试改 HTML/JS/CSS：

```bash
go run .
```

- 如果需要从源码临时测试“编译时快照/内嵌资源”的行为，使用：

```bash
go run . -static embedded
```

注意事项：

- `webllm-gateway-config.json` 仍写入当前工作目录，并由 gateway 从磁盘优先提供给页面读取。
- 修改前端资源后，如果要更新单文件二进制，必须重新运行构建脚本或 `go build .`。
- Go `embed` 的路径不能引用 package 目录之外的文件，因此采用“根目录 main.go 负责 embed，gateway 包负责服务逻辑”的结构。
- 构建脚本在 `embedded` 模式下通过 `-ldflags "-X webllm-serve/gateway.DefaultStaticMode=embedded"` 把 release 二进制的默认静态模式改为 embedded；源码直接 `go run .` 时默认仍是 local。

### 双 release 模式

当前构建脚本保留两种发布形态：

- `embedded`：默认模式。构建内嵌静态资源的单文件 gateway，release 包只需要二进制和 `README.md`。
- `files`：传统文件模式。使用 `-tags localstatic` 构建读取本地文件的 gateway，并把 `index.html`、`server.html`、`client.html`、同源页面和 `src/` 一起复制到 release 包。

构建命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\build-gateway.ps1
powershell -ExecutionPolicy Bypass -File scripts\build-gateway.ps1 -PackageMode files
```

```bash
sh scripts/build-gateway.sh
sh scripts/build-gateway.sh dist release files
```

使用建议：

- 给普通用户优先发布 `embedded`，下载一个可执行文件即可运行。
- 如果希望用户能直接修改 HTML/JS/CSS，或需要复现旧版目录结构，则发布 `files`。
- `files` 模式下必须从 release 包目录运行 gateway，否则找不到本地页面文件。

### release 打包

- 构建脚本位于 `scripts/build-gateway.ps1` 和 `scripts/build-gateway.sh`。
- 默认构建三个主流发布包：
  - `release/webllm-serve-windows-amd64/`
  - `release/webllm-serve-macos-arm64/`
  - `release/webllm-serve-linux-amd64/`
- 每个发布包内包含统一入口：
  - Windows：`webllm-gateway.exe`
  - macOS/Linux：`webllm-gateway`
- 发布包还会复制 `index.html`、`server.html`、`client.html`、同源验证页面、`src/` 和 `README.md`，所以用户进入对应目录后可以直接运行 gateway。
- `release/` 已加入 `.gitignore`，当前阶段只作为本地测试生成物，最终版确认后再考虑上传。
- `dist/` 保留裸二进制输出；默认只更新 `windows-amd64`、`darwin-arm64`、`linux-amd64` 三个目标。
- 可选目标保留在脚本中，默认关闭。需要全量构建时：
  - PowerShell：`powershell -ExecutionPolicy Bypass -File scripts\build-gateway.ps1 -AllTargets`
  - sh：`ALL_TARGETS=1 sh scripts/build-gateway.sh`

注意事项：

- 如果手动删除 `dist/` 和 `release/`，重新运行默认构建脚本即可恢复当前默认产物。
- 不建议把 release 目录直接长期纳入 Git；发布前统一重新构建更清晰。
- 构建脚本结尾如果出现 Conda 的 UnicodeEncodeError，在本机环境里通常是 PowerShell 自动激活 conda 的编码问题，不代表 Go 构建失败；以脚本是否输出 `Done` 和退出码为准。

### 页面双语化

- 新增 `src/i18n.js`，集中维护中英文文案。
- 新增 `src/home.js`，负责首页语言初始化和读取 gateway 实际端口配置。
- 主线页面已接入语言切换：
  - `index.html`
  - `server.html`
  - `client.html`
- 静态文案通过 `data-i18n="key"` 标记。
- placeholder 使用 `data-i18n-placeholder="key"`。
- aria-label 使用 `data-i18n-aria-label="key"`。
- JS 动态文案使用 `t("key")` 或 `t("key", { name: value })`。
- 语言选择会写入 `localStorage`，因此在首页切换语言后，server/client 主线页面会沿用同一语言。

后续开发约定：

- 新增 UI 文案时，需要同时在 `src/i18n.js` 的 `zh` 和 `en` 中补 key。
- 页面静态文本优先用 `data-i18n`，运行时状态、错误、日志提示优先用 `t()`。
- 目前双语化覆盖主线页面；`server-same-origin.html`、`client-same-origin.html` 仍作为同源验证版本保留，后续可按同一套机制迁移。
- 不建议依赖浏览器自动翻译技术页面，因为模型名、API 字段、错误信息容易被翻译错。

## 阶段 5：Jupyter AI / Tools 兼容性探索

本阶段主要目标不是立即完成“Jupyter AI tools 可稳定使用”，而是确认 gateway、WebLLM、模型和 Jupyter AI 之间的责任边界，并记录哪些方案只是临时验证，不适合作为主线。

### 现象

Jupyter AI 未开启 Tools 时，请求只包含 `messages`、`model`、`stream`、`temperature` 等常规字段。即使 Jupyternaut 的 system prompt 很长，普通问答仍然可以收到回复。

开启 Tools 后，Jupyter AI 会额外传入：

```json
{
  "tool_choice": "auto",
  "tools": [...]
}
```

诊断时看到的典型请求规模：

```json
{
  "model": "Hermes-2-Pro-Mistral-7B-q4f16_1-MLC",
  "stream": true,
  "tool_choice": "auto",
  "messageCount": 2,
  "messageChars": [12536, 16],
  "systemChars": 12536,
  "toolCount": 5,
  "toolsChars": 2686,
  "requestChars": 15648,
  "hasToolResult": false
}
```

其中 `systemChars` 主要来自 Jupyter AI / Jupyternaut 的长 system prompt，`toolsChars` 来自 Jupyter AI 传入的 OpenAI 风格 tool schema。

### 关键结论

- WebLLM 不是完全不支持 tool calling。官方 README 将 function calling 标为 WIP / preliminary，并说明支持 `tools` 和 `tool_choice` 字段，也支持不走 OpenAI `tools` 字段的 manual function calling。
- 但 WebLLM 的 tool calling 能否稳定工作，强依赖模型、chat template 和 runtime 处理逻辑。不能简单理解为“只要有 `tools` 字段就一定能像 OpenAI API 一样工作”。
- Hermes-2-Pro 在一个最小 curl 测试中，不传自定义 system prompt、只传简单 tools 时，可以返回 `finish_reason=tool_calls`。这说明 Hermes / WebLLM 的基础 tool calling 通路是能启动的。
- Hermes-2-Pro 在 Jupyter AI 场景下失败的核心原因是：`tools` 与 Jupyter AI 自定义 system prompt 同时存在时，WebLLM 抛出错误：

```text
When using Hermes-2-Pro function calling via ChatCompletionRequest.tools, cannot specify customized system prompt.
```

这个错误来自浏览器里的 WebLLM JS 运行时，而不是 Jupyter AI 或 gateway 主动返回的业务错误。对应的上游讨论见：

- https://github.com/mlc-ai/web-llm/issues/462

该 issue 明确讨论了当前 function calling 场景里不能同时提供 system prompt 与 tools，以及是否可以把用户提供的 system prompt 与 Hermes 官方 tool prompt 合并。

### 尝试过的方案

曾经临时加入过一个 UI 开关：

```text
Drop system messages when tools are present
```

这个开关的作用是：当请求里存在 `tools` 时，调用 WebLLM 前删除 Jupyter AI 传来的 `role: "system"` messages。它不会删除 Hermes / WebLLM 内部的模型 tool prompt，只是屏蔽 Jupyter AI 的 Jupyternaut system prompt。

这个实验可以证明：去掉 Jupyter AI system prompt 后，Hermes 的 WebLLM tool calling 通路可以继续往前走。但是实际效果不可靠：

- 模型失去了 Jupyter AI 的工具使用规则和 notebook 操作说明。
- 容易混淆 `discover_commands`、`discover_skills`、`execute_command`。
- 曾出现 `create an empty txt file` 时输出不完整 tool call，并持续生成空白内容。
- 对 Jupyter AI 来说，删除 system prompt 会破坏它对工具调用、notebook 状态、kernel 使用策略的约束。

因此这个开关只适合作为实验验证，不适合作为主线能力。后续已从主线 UI 中移除。

### Gateway 边界

gateway 的职责应保持为：

- 负责 HTTP / SSE / WebSocket 通信。
- 做必要的 OpenAI 风格请求与 WebLLM 请求之间的格式兼容。
- 记录诊断信息，帮助判断请求规模、tools 数量、system prompt 长度和模型返回错误。
- 在 WebLLM 模型由浏览器 `engine.reload(model)` 加载的前提下，调用 WebLLM 时不依赖客户端传入的 `model` 字段。

gateway 不应该默认做这些事：

- 不应该静默删除 Jupyter AI 的 system prompt。
- 不应该静默删除 `tools` 或 `tool_choice`。
- 不应该为了让某个模型不报错而伪造、压缩、改写 Jupyter AI 的工具说明。
- 不应该在未标记为实验功能的情况下，把 Jupyter AI 的 system message 改成 user message。

如果某个模型 / runtime 组合不兼容，例如 Hermes-2-Pro + `tools` + 自定义 system prompt，主线更合理的做法是保留原始请求语义，暴露清晰错误和诊断信息，而不是偷偷改 prompt。

### Qwen 相关观察

Qwen 模型在普通问答上比 Hermes 更正常，开启 Tools 后也没有触发 Hermes 那个“不能自定义 system prompt”的错误。但这不等于已经确认 Qwen 可以稳定支持 Jupyter AI tool calling。

实际测试中还看到过上下文限制问题：

```text
Prompt tokens exceed context window size: number of prompt tokens: 5145; context window size: 4096
```

Jupyter AI 的 system prompt 与 tools schema 本身就比较长，因此小上下文模型即使不报 function-calling 模板错误，也可能因为上下文窗口不足而失败。对推荐模型可以用 `context_window_size: 8192` 这类 chat option 做实验，但它会增加显存 / 内存压力，也可能变卡。

### web-agent-interface 的参考价值

`mlc-ai/web-agent-interface` 可以作为思路参考，因为它代表了另一类做法：不完全依赖 OpenAI 风格 `tools` 字段，而是在 agent 层用手动协议、结构化输出和解析执行来完成工具调用。

参考：

- https://github.com/mlc-ai/web-agent-interface
- https://github.com/mlc-ai/web-llm

但本项目当前主线目标是让 gateway 尽量作为通信与必要格式转换层，而不是在 gateway 内部接管 agent 协议。因此 web-agent-interface 的思路可以用于后续实验分支，不应直接混入主线 gateway 行为。

### 暂定结论

Jupyter AI + WebLLM + Tools 的问题不是单纯的 Windows、请求长度或 gateway 转发问题，而是多因素叠加：

- Jupyter AI 会发送很长的 Jupyternaut system prompt。
- 开 Tools 后会额外发送 OpenAI 风格 `tools` schema。
- WebLLM 的 OpenAI 风格 function calling 仍是 WIP / preliminary。
- Hermes-2-Pro 的 WebLLM tool calling 路径当前不支持同时传自定义 system prompt。
- 删除 Jupyter AI system prompt 可以绕过 Hermes 限制，但会让工具调用缺少 Jupyter AI 的关键行为约束，实测不可靠。

后续如果继续推进，建议分两条线：

1. 主线保持 gateway 透明：转发 `tools` / `tool_choice`，不改 Jupyter AI prompt，只做必要格式转换和诊断。
2. 实验分支研究模型专用 tool template 或 manual tool calling：例如参考 Hermes Function Calling、WebLLM issue #462 和 web-agent-interface，但要明确标记为实验能力，避免污染主线兼容层。


## 阶段 6：模型 alias、默认模型与 Server UI 可用性优化

本阶段围绕“模型数量会增长到 100+”这一前提，优化了模型选择、API 调用和文档示例。重点不是增加模型推理能力，而是降低调用和维护成本。

### 稳定 alias 机制

模型清单从单纯的 `MODEL_IDS` 升级为 `MODEL_CATALOG`：

```js
{ alias: "default", id: "Qwen3.5-2B-q4f16_1-MLC" }
{ alias: "m001", id: "gemma3-1b-it-q4f16_1-MLC" }
```

设计要点：

- `default` 是额外别名，不占用原模型的固定编号。
- 当前 `default` 指向 `Qwen3.5-2B-q4f16_1-MLC`。
- 同一个 `model id` 允许出现多次，因此 `Qwen3.5-2B-q4f16_1-MLC` 可以同时拥有 `default` 和普通编号 alias。
- 以后更换默认模型时，只需要修改 `MODEL_CATALOG` 第一条 `default` 的 `id`。
- `current` 和 `loaded` 保留为特殊别名，表示 `server.html` 当前已加载模型。

API 调用现在可以使用：

```json
{ "model": "default" }
```

也可以使用：

```json
{ "model": "m009" }
```

或直接使用完整 `model id`。不要再支持裸数字或 `#数字` 之类推断写法，避免以后使用 `mm1` 等 alias 命名时产生歧义。

### Server UI 模型选择优化

`server.html` 的模型选择从原生长下拉改为轻量自定义 combobox：

- 默认只显示当前选中模型，避免搜索框喧宾夺主。
- 点开后显示搜索框和模型列表。
- 搜索只匹配 `alias` 和 `model id`。
- 模型列表内部滚动，不撑破页面。
- 窗口高度较小时，下拉面板会根据空间自动调整高度，必要时向上展开。
- 每个模型选项设置稳定最小高度，避免窗口高度低时列表行被压扁。

这类 UI 逻辑集中在：

```text
server.html
src/server.js
src/styles.css
```

### API 示例更新

首页 `index.html` 和 `src/home.js` 中的 OpenAI CLI / Python SDK / Node SDK 示例统一使用：

```text
model = "default"
```

原因：

- `local` 只是早期“本地模型”的占位说法，不是有效 alias。
- `default` 更适合作为用户复制即用的入口。
- `server.html` 的 Python API Client 示例在已加载模型后使用 `loaded`，这样不会因为默认模型指向 Qwen3.5-2B 而误导用户。

### README 与首页启动命令同步

项目入口已经改为根目录 `main.go`，因此开发启动命令应统一为：

```bash
go run .
```

旧命令：

```bash
go run ./gateway
```

已经不再作为主线说明使用。`README.md` 和 `index.html` 均已更新。后续文档中如果出现 gateway 启动命令，应优先使用 `go run .`，并说明要从仓库根目录运行。

### 验证

本阶段涉及前端 JS、Go gateway 和文档，主要检查命令：

```bash
node --check src/home.js
node --check src/models.js
node --check src/server.js
node --check src/i18n.js
go test ./...
git diff --check
```

## 阶段 7：模型元数据、预置模型查看页与发布说明整理

这一阶段主要围绕“模型信息可见性”和“发布/使用说明一致性”做增强，目标是让后续添加大量 WebLLM 模型时，开发者和用户都能更容易判断模型、别名、显存需求和来源。

### 模型列表与 VRAM 展示

`src/models.js` 从单纯的 `model id` 列表扩展为带元数据的 `MODEL_CATALOG`：

```js
{ alias: "m009", id: "Qwen3.5-2B-q4f16_1-MLC", vramMB: 2245.44 }
```

关键处理：

- Server UI 的模型显示格式改为 `alias | model id | VRAM`。
- `VRAM` 以 `VRAM ~x.x GB` 或 `VRAM ~xxx MB` 展示，提醒这是 WebLLM 的估算需求，不是严格物理显存占用。
- 当前常用模型的 `vramMB` 已从 WebLLM 官方 `prebuiltAppConfig.model_list` 中补齐。
- 页面启动时仍会异步读取 `webllm.prebuiltAppConfig.model_list`，用于补充/更新内置模型元数据；网络慢或失败时不影响本地 `MODEL_CATALOG` 的基本使用。

注意：自动读取 WebLLM 配置时，要严格按 `model_id` 所在 record 匹配 `vram_required_MB`。简单截取字符串容易把相邻模型的 VRAM 抓错。

### 完整 Prebuilt Model Inspector

新增独立页面：

```text
prebuilt-models.html
src/prebuilt-models.js
```

用途：

- 异步加载 `@mlc-ai/web-llm@0.2.83`。
- 读取 `webllm.prebuiltAppConfig.model_list`。
- 展示整理后的表格。
- 展示原始 JSON。
- 支持搜索、表头排序、列宽拖拽。
- 表格和 JSON 都支持纵向展开/收起高度。

页面参考来源：

```text
https://llm.mlc.ai/docs/deploy/webllm.html#model-records-in-webllm
```

UI 细节经验：

- 表格 URL 列如 `model` / `model_lib` 很长，应降低字号并给更宽的列宽，由表格容器自身提供横向滚动。
- 不要让宽表格撑开整个页面。关键是让外层 grid/panel 子项允许收缩：

```css
.model-inspector > * {
  min-width: 0;
}
```

- 表格自身可以使用 `width: max-content; min-width: 100%;`，横向滚动放在 `.model-table-wrap` 内部。
- JSON 区域可以限制到窗口宽度并换行，避免制造页面级横向滚动。

### Server UI 代码和日志展示

`server.html` 接入 Prism 高亮：

- Python API Client 示例使用 `language-python`。
- Request/Response Logs 中的结构化对象使用 `language-json`。
- stream 正文保持普通连续文本；latest chunk 使用 JSON 高亮。

注意事项：

- Prism 主题可能直接覆盖 `code[class*="language-"]` 和 `.token` 的字号。若要控制 JSON 字体大小，需要同时设置 `pre`、`code` 和 `.token`。
- CDN 失败时，主页面和 server 页面保留本地 Prism fallback；`prebuilt-models.html` 的 JSON 高亮按当前设计只使用在线 Prism，不做本地 fallback。

### 构建与发布文档调整

当前 `dist/` 和 `release/` 都作为本地构建产物处理，不再提交到 git。

README 已调整为：

- 下载 release package 的用户：进入对应平台目录，直接运行 `webllm-gateway` 或 `webllm-gateway.exe`。
- git clone 的开发者：默认没有二进制文件，使用 `go run .` 开发。
- 需要二进制时，运行构建脚本生成本地 `dist/` 和 `release/`。

重要说明：

```text
webllm-gateway-config.json
```

这是 gateway 运行时生成的配置发现文件，不是 Go 单文件释放出来的静态资源。它通常写入当前工作目录，用于让页面读取实际端口和 WebSocket 地址，因此应继续被 `.gitignore` 忽略。

### 打包注意事项

新增页面后，需要同步维护：

```text
static_embed.go
scripts/build-gateway.ps1
scripts/build-gateway.sh
```

原因：

- embedded 模式需要 `static_embed.go` 包含新 HTML。
- files 模式需要构建脚本复制新 HTML。
- 如果忘记更新，开发模式 `go run .` 可能正常，但 release 包里会缺页面。

### 验证命令

本阶段常用检查：

```bash
node --check src/prebuilt-models.js
node --check src/models.js
node --check src/server.js
node --check src/i18n.js
go test ./...
git diff --check
```
